# test/test.yaml
#
# Minimal but complete Aegis configuration used by the integration test.
# All fields required for a valid startup configuration are present.
# Memory (data and status) is derived from replicator.units[*].target at startup.

replicator:
  units:
    - id: "plc1"
      source:
        endpoint: "192.168.1.100:502"
        unit_id: 1
        timeout_ms: 1000
        device_name: "PLC1"

      reads:
        - fc: 3               # Read Holding Registers
          address: 0
          quantity: 10
          interval_ms: 1000

      target:
        port: 502
        unit_id: 1
        status_unit_id: 255
        status_slot: 0        # block index 0 → base register 0 in status memory
        mode: "B"
